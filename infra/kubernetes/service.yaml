apiVersion: v1
kind: Service
metadata:
  name: ml-training-serving-platform
spec:
  selector:
    app: ml-training-serving-platform
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
